{% extends 'base.html.twig' %}

{% block title %}Véhicules{% endblock %}

{% block body %}
    <div class="div-liste">
        <div id="div-tab">
            {% if errors is defined %}
                {% for error in errors %}
                    <div class="alert alert-danger" role="alert">{{ error.message|nl2br }}</div>
                {% endfor %}
            {% endif %}
            {{ form_start(formModificationVehicule) }}
            <div class="div-detail mb-3">
                {{ form_label(formModificationVehicule.fk_client) }}
                <div class="col-lg-5 col-md-6">
                    {{ form_widget(formModificationVehicule.fk_client) }}
                </div>
            </div>
            <div class="div-row mb-3 gap-3">
                <div class="div-select-line">
                    {{ form_label(formModificationVehicule.fk_marque) }}
                    {{ form_widget(formModificationVehicule.fk_marque) }}
                </div>
                <div class="div-select-line">
                    {{ form_label(formModificationVehicule.fk_modele) }}
                    {{ form_widget(formModificationVehicule.fk_modele) }}
                </div>
            </div>
            <div class="div-detail mb-3">
                {{ form_label(formModificationVehicule.fk_carburant) }}
                <div class="col-lg-5 col-md-6">
                    {{ form_widget(formModificationVehicule.fk_carburant) }}
                </div>
            </div>
            <div class="div-detail mb-3">
                {{ form_label(formModificationVehicule.annee) }}
                <div class="col-lg-5 col-md-6">
                    {{ form_widget(formModificationVehicule.annee) }}
                </div>
            </div>
            <div class="div-detail mb-3">
                {{ form_label(formModificationVehicule.kilometrage) }}
                <div class="col-lg-5 col-md-6">
                    {{ form_widget(formModificationVehicule.kilometrage) }}
                </div>
            </div>
            <div class="div-detail mb-3">
                {{ form_label(formModificationVehicule.immatriculation) }}
                <div class="col-lg-5 col-md-6">
                    {{ form_widget(formModificationVehicule.immatriculation) }}
                </div>
            </div>
        </div>
        <div class="div-envoi-form">
            <button type="submit" class="btn btn-success">Valider</button>
        </div>
        {{ form_end(formModificationVehicule) }}
    </div>
{% endblock %}