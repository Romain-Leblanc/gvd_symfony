{% extends 'base.html.twig' %}

{% block title %}Interventions{% endblock %}

{% block body %}
    <div class="div-liste">
        <div class="div-table">
            <table class="table table-hover table-bordered align-middle text-center">
                <thead class="thead-light align-middle">
                    <tr>
                        <th scope="col"></th>
                        <th scope="col">N° inter.</th>
                        <th scope="col">Date intervention</th>
                        <th scope="col">Voiture</th>
                        <th scope="col">Client</th>
                        <th scope="col">Montant HT</th>
                    </tr>
                </thead>
                <tbody>
                {% for uneIntervention in lesInterventions %}
                    <tr>
                        <td id="radio-button-table">
                            <input type="radio" class="form-check-input" id="radio-input" name="radio-input" />
                        </td>
                        <td data-label="N° inter.">{{ uneIntervention.id }}</td>
                        <td data-label="Date intervention">{{ dateEnFrancais(uneIntervention.getDateIntervention) }}</td>
                        <td data-label="Voiture">{{ marqueModele(uneIntervention.getFKVehicule.getFKMarque.getMarque,uneIntervention.getFKVehicule.getFKModele.getModele) }}</td>
                        <td data-label="Client">{{ nomPrenom(uneIntervention.getFKClient.getNom,uneIntervention.getFKClient.getPrenom) }}</td>
                        <td data-label="Montant HT">{{ formatMontantEuros(uneIntervention.getMontantHT) }}</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
        <div class="text-end fw-bold mt-2">
            <p class="mb-0">{{ lesInterventions|length}} intervention{{ affichagePlusieursValeurs(lesInterventions|length) }}</p>
        </div>
    </div>
{% endblock %}
